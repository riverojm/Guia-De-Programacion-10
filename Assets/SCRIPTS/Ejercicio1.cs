using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int num = 0;
        num =+ 2;
        Debug.Log("la variable vale= "+num+"");
        num = num * num;
        Debug.Log("la variable vale= " + num + "");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
